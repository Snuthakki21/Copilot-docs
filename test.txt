Option 3: Keep all rows, but add two flag columns:

Business_Relevant — Yes/No/Maybe. Yes = LOB-Product contains "Small Business" OR "Business Banking" OR "BBG" OR "Commercial," OR Folder contains those terms, OR Description/Name explicitly references business ownership, business checking, business deposit, or small business segmentation. No = LOB-Product is explicitly Consumer/Personal only with no business reference anywhere. Maybe = ambiguous, applies to General/Multiple LOBs, or unclear from available fields.
Business_Match_Reason — short text explaining which field triggered the Yes or Maybe (e.g., "Folder: Small Business," "LOB: Small Business - General/Multiple," "Description references primary owner of business checking").

Apply this to all three searches, not just Search 1, so they're consistent. Sort each output with Yes rows first, Maybe second, No last. Confirm row counts per flag value at the end.
Do not drop, merge, or paraphrase any rows. Keep all original columns intact and add the two flag columns at the end.
